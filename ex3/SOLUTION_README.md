Compile and run using the following commands. The input parameters are provided interactively during the execution of the program.
```bash
gcc main.c -o main.out

./main.out

```
The program generates a `C.txt` file containing the resulting matrix.